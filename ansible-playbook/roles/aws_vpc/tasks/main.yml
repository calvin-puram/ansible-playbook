- name: Add runner user
  user:
    name: runner
    groups: sudo
    state: present

- name: Create a directory if it does not exist
  become: yes
  become_user: runner
  file:
    path: /etc/some
    state: directory
    mode: "0755"
